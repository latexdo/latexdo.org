{
  "name": "latexdo-org",
  "version": "0.1.0",
  "private": true,
  "description": "Static TypeScript website for LatexDo.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:downloads": "node scripts/check-downloads.mjs",
    "check:footers": "node scripts/check-footers.mjs",
    "check:product-demo": "node scripts/check-product-demo.mjs",
    "dev": "serve -l 8787 .",
    "preview": "serve -l 8787 .",
    "test": "npm run typecheck && npm run build && npm run check:product-demo && npm run check:footers && npm run check:downloads",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "serve": "^14.2.6",
    "wrangler": "^4.130.0"
  }
}
